version: "2"

volumes:
  grafana-storage:
    driver: local

services:
  grafana:
    image: grafana/grafana:6.5.0-ubuntu
    ports:
      - 3000:3000
    user: "472"
    volumes:
      - grafana-storage:/var/lib/grafana
