version: "3"

services:
  jenkinsci:
    image: "jenkins/jenkins"
    container_name: "jenkinsci"
    ports:
      - 8080:8080
      - 50000:50000
    volumes:
      - ./jenkins-data:/var/jenkins_home
